import { View, Heading } from 'native-base';

export default function TeamScreen() {
  return (
    <View justifyContent={'flex-end'} alignItems={'flex-start'}>
      <Heading> Team Works!</Heading>
    </View>
  );
}
